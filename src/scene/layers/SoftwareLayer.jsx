/* eslint-disable react/prop-types */
import ModelLayer from './ModelLayer';

function SoftwareLayer(props) {
    return <ModelLayer {...props} modelId="software" />;
}

export default SoftwareLayer;
